# 8010 网页端 LLM 各阶段回答倾向与结构

> 更新时间：2026-08-13
> 对应提示词版本：`cocreation-v15-natural-optional-questions`

## 1. 文档范围

本文总结 8010 共创网页端在当前代码下的 LLM 调用阶段、回答倾向、结构化输出、前端呈现和关键限制。主要依据：

- [`CoCreationPrototype/Backend/llm_client.py`](CoCreationPrototype/Backend/llm_client.py)：提示词、模型选择、结构校验与回答拼装；
- [`CoCreationPrototype/Backend/app.py`](CoCreationPrototype/Backend/app.py)：Stage、对话、提案、试玩和最终确认的接口流程；
- [`CoCreationPrototype/Frontend/app.js`](CoCreationPrototype/Frontend/app.js)：自动首轮评价、消息呈现、彩色提示块与提案交互；
- [`AGENTS.md`](AGENTS.md)：当前已经确认的研究和产品方向。

这里的“回答倾向”主要是根据当前提示词和程序约束推断的行为倾向，不是对线上真实回答频率的统计。本地 `data/cocreation.sqlite3` 当前没有可供抽样的会话轮次。

## 2. 总体角色与共同回答倾向

LLM 被定义为平等、审慎的 Sokoban 设计伙伴，而不是评分员、问卷主持人或一味赞同用户的助手。所有阶段共同遵循以下倾向：

- 只讨论当前已保存 Stage、该 Stage 的相关对话、确定性求解证据和最近一次试玩证据；
- 根据语境选择简短直接回答、观察、联想、不同意见或较长反思，不固定排列顺序；
- 每次选择一个主要对话动作，不试图在一条回答中完成所有任务；
- 关卡是否可解、最少步数和推动次数以确定性验证为准；
- 难度、趣味性和设计效果只能作为 LLM 的主观看法表达；
- 不替设计者指定“竞争、支持、困难、友好”等预设目标；
- 可以推测设计意图，但必须是直接对设计者说出的、可纠正的暂定假设；
- 普通回答为一至三个短段落，最多包含一个核心问题；
- 开场和普通轮次都可以不提问，只有问题的答案确实会改变下一步讨论或行动时才应提问。
- 对话上下文只取当前 Stage 最近 24 个 turn；历史 Stage 的对话不会混入当前 Stage；
- 唯一的跨 Stage 延续是：接受 LLM 提案后，生成该提案的 assistant turn 会作为新 Stage 的 carried opening。

当前可选的主要对话动作如下：

| `guidance.move` | 典型倾向 |
| --- | --- |
| `observe_stage` | 观察当前布局中的一两个具体特点 |
| `clarify_intent` | 澄清设计者刚表达的意图或判断 |
| `offer_perspective` | 提供独立的设计观点或关联 |
| `challenge_tradeoff` | 指出一个值得权衡的潜在风险 |
| `reflect_on_play` | 结合当前 Stage 的最新试玩证据进行反思 |
| `offer_revision` | 提出修改方向和理由，但不生成完整地图 |
| `deliver_revision` | 在获得明确授权后生成完整地图提案 |

## 3. 所有回答共用的结构化输出

LLM 必须返回一个字段完全固定的 JSON 对象：

```json
{
  "assistantMessage": "自然语言正文",
  "guidance": {
    "move": "observe_stage",
    "intentHypothesis": null,
    "intentConfidence": null,
    "followUpQuestion": null,
    "proposalOffer": null,
    "uiCues": []
  },
  "assessment": null,
  "proposedRows": null,
  "modificationSummary": ""
}
```

各字段的作用如下：

| 字段 | 用途 | 主要限制 |
| --- | --- | --- |
| `assistantMessage` | 正常对话正文 | 必须非空；不能只有问题；问题会被抽取到 `followUpQuestion` |
| `guidance.move` | 本轮主要对话动作 | 必须是七种允许值之一 |
| `intentHypothesis` | 暂定设计意图 | 必须使用“我猜你可能……”等第一、第二人称可纠正表达 |
| `intentConfidence` | 意图推测置信度 | 仅在存在意图推测时使用 `low`、`medium` 或 `high` |
| `followUpQuestion` | 本轮唯一的跟进问题 | 可以为空；非空时必须正好是一个问题 |
| `proposalOffer` | 文字层面的修改方向 | 仅能与 `offer_revision` 同时出现，结构固定为 `summary` 和 `rationale` |
| `uiCues` | 手动编辑提示或风险提示 | 最多两个；当前新输出只使用 `manual_edit` 或 `warning` |
| `assessment` | 新 Stage 的归档评价 | 普通对话按提示词应为空；Stage 开场必须为完整对象 |
| `proposedRows` | 完整地图提案 | 只允许在 `deliver_revision` 中出现，必须是 10 行、每行 12 格 |
| `modificationSummary` | 完整提案的实际改动摘要 | 必须与新旧地图的真实差异一致 |

如果模型把问题写进正文，后端会按句子抽取。正文最多只能含一个问题，并且抽取后仍必须保留陈述内容。

## 4. 前端实际呈现结构

助手消息在网页中的显示顺序是：

```text
普通正文
└─ 暂定意图（橙色，可选）
└─ 手动编辑提示（绿色，可选）
└─ 风险警告（红色，可选）
└─ 修改方向或提案（紫色，可选）
└─ LET'S DISCUSS / 一起聊聊（蓝色，可选）
```

前端会从正文末尾移除已经结构化的提示和问题，避免同一句重复显示。彩色提示块的固定标签为：

| 内容 | 英文标签 | 中文标签 | 颜色 |
| --- | --- | --- | --- |
| 手动编辑建议 | `MANUAL EDIT` | `手动编辑` | 绿色 |
| 跟进问题 | `LET'S DISCUSS` | `一起聊聊` | 蓝色 |
| 修改方向或提案 | `REVISION` | `修改建议` | 紫色 |
| 暂定意图 | `TENTATIVE INTENT` | `暂定意图` | 橙色 |
| 风险或取舍 | `WARNING` | `警告` | 红色 |

## 5. 各 Stage 来源的开场回答

除“接受 LLM 提案生成的新 Stage”以外，每个新保存 Stage 都会自动请求一次开场评价。开场调用使用 `assessment_only=True`，并具有比普通对话更严格的统一结构：

1. `move` 必须为 `observe_stage`；
2. 正文使用一至三个短段落，长度和节奏按内容决定；
3. 只选择一两个值得讨论的具体设计点；
4. 至少给出一个明确标记为主观看法的观点、联想或温和担忧；
5. 问题可为空；如确实需要，只能有一个开放问题；
6. 不得推测设计意图、提出修改方向、生成地图或添加额外 UI cue；
7. `assessment` 必须同时生成，但只作为研究归档数据，不作为正文写作模板。

开场 `assessment` 固定包含 `solutionSummary`、`difficultyOpinion`、`features`、`suggestions` 和 `satisfactionQuestion`。其中 `satisfactionQuestion` 是兼容旧数据结构的可空字段，必须与 `followUpQuestion` 完全一致：两者可以同时为 `null`。

### 5.1 PC 初始 Stage：`initial + partial_completion`

设计归属：

- 设计者控制箱子起点、目标点和较宽泛的草图／房间约束；
- 补全系统加入精确水域、生成的内部墙、玩家起点和其余布局；
- 最终 rows 无法分辨某一格内部墙究竟来自草图还是补全。

LLM 的回答倾向：

- 可以讨论箱子与目标的关系；
- 可以讨论较宽泛的房间边界或草图约束；
- 可以询问生成结果是否支持或偏离了原草图；
- 不应说“你放置了这面内部墙、水域或玩家”；
- 不应把所有墙都归因于补全系统。

典型可见结构：

```text
对一个或两个当前布局特征的观察

LLM 的主观看法、联想或轻微担忧

[一起聊聊，可选] 只有确实需要设计者输入时，才提出围绕箱子—目标关系、房间边界或补全效果的开放问题
```

### 5.2 DG 初始 Stage：`initial + description_generation`

设计归属：

- 设计者只控制上游文字描述和生成参数；
- 生成器决定所有精确格子位置；
- 8010 没有收到具体参数值。

LLM 的回答倾向：

- 把墙、水、箱子、目标、玩家和走廊描述为“生成结果”；
- 询问结果与设计者对参数效果的预期如何比较；
- 可以询问什么结果令人意外，或设计者想加强／修正什么效果；
- 不应询问“为什么你把某块墙或水放在这里”；
- 不应虚构或引用 8010 没有收到的参数值。

DG 如果使用开场问题，应偏向“对生成结果的反应”，而不是“解释某个格子的放置动机”。不需要追问时可以只表达观察和观点。

### 5.3 人工编辑 Stage：`human_edit`

创建条件：设计者在右侧地图编辑器修改并保存了一个与当前 Stage 不同、且通过确定性验证的地图。

回答倾向与结构：

- 后端先比较父 Stage 和新 Stage，分类外壳、水域、内部墙、箱子、目标、玩家和可用地面等实际变化；
- 后端会在 LLM 正文前强制加入一段确认，例如“我注意到你对……进行了修改。这个已保存的 Stage 已通过确定性检查并确认可解”；
- LLM 只能把确定检测到的变更归因于设计者，不能猜测未记录的动机；
- 随后评价这些变化可能产生的设计效果，并继续一个新的开场讨论；
- 不强制附带开放问题；需要设计者输入时才显示蓝色问题卡片。

因此该阶段的可见回答通常比其他开场多一个固定确认段，整体可能显示为三至四段。

### 5.4 恢复历史 Stage：`restored`

创建条件：设计者从一个只读历史 Stage 选择“从这个 Stage 继续”，系统复制其地图并创建新的当前 Stage。

LLM 的回答倾向：

- 把当前状态表述为“重新回到／继续探索一个历史版本”；
- 不推断某个精确格子最初由谁放置；
- 重新生成一次中性、基于当前地图的开场评价；
- 使用一至三个短段落；开放问题可选。

### 5.5 接受 LLM 提案后的 Stage：`llm_accepted`

这是唯一不会再生成一次开场评价的新 Stage。

- 原先生成该地图提案的 assistant turn 会成为新 Stage 的 carried opening；
- 该 turn 不会被复制或改写，仍保留原始版本和研究关系；
- 新 Stage 的后续聊天直接从已接受的共同方向继续；
- LLM 应称其为“已接受的共同方向”，不能说成设计者亲手放置了所有格子，也不能说 LLM 已经擅自改图。

这种设计避免出现“刚接受提案，助手马上又从头评价同一张图”的重复开场。

## 6. 普通多轮讨论中的回答类型

### 6.1 观察当前 Stage：`observe_stage`

倾向于从当前地图选择少量具体关系展开，例如路线、空间限制、箱子—目标关系或水域造成的绕行。不会逐项盘点整张地图。

### 6.2 澄清意图：`clarify_intent`

当设计者的表达存在歧义、需要补充信息或需要纠正 LLM 理解时使用。可以提出一个问题，但普通轮次没有强制要求提问。

### 6.3 提供独立观点：`offer_perspective`

LLM 可以直接回应用户，也可以让一个有价值的设计判断、联想或不同看法独立成答，不再强制固定顺序。它仍被要求不要机械赞同，且所有难度或体验判断都要标记为主观观点。

### 6.4 挑战取舍：`challenge_tradeoff`

用于指出当前结构、版本变化、求解指标或试玩证据中值得注意的风险。

- 必须附带正好一个红色 `warning` cue；
- 风险阈值适中，不必等风险已被确定才提醒；
- 可以涉及死锁／软锁、推动顺序、路线可读性、目标理解、重复移动或与设计者方向的冲突；
- 不允许使用没有地图或证据支持的猜测；
- 正文不能再重复同一条警告。

### 6.5 暂定意图推测

这不是独立 Stage，而是普通讨论中可附加的结构：

- 只有对话或设计操作提供证据时才使用；
- 中文必须类似“我猜你可能……”或“我感觉你似乎……”；
- 英文必须类似 `I think you may...`；
- 第三人称报告式表达会被后端自动改写；
- 显示为橙色“暂定意图”；
- 永远不能自动写入设计者最终意图。

### 6.6 手动编辑提示

当直接试验比继续抽象讨论更有帮助时，LLM 可以简短提醒右侧编辑器。

- 完整提示放进绿色 `manual_edit` cue；
- 不在正文重复；
- 不应每轮机械提醒；
- 不应暗示人工编辑是必须步骤。

## 7. 试玩后的回答行为

试玩完成、提前返回或中断本身不会自动生成一条 LLM 回复。

最近一次同 Stage 试玩数据会在下一次 LLM 调用时加入上下文，包括：

- 状态：完成或提前返回；
- 用时；
- 移动数、推动数和重开数；
- 最少移动数和最少推动数；
- 完成时间。

当这些证据与设计者的新消息相关时，LLM 倾向使用 `reflect_on_play`：

- 回应设计者对试玩的描述，并按语境决定是否补充独立观点；
- 再把主观体验和记录到的客观指标区分开；
- 只在能支持有用观察时引用步数或推动数；
- 不展示完整求解移动序列；
- 不把一次试玩结果直接当成确定的难度结论。

## 8. 修改建议与完整地图提案

### 8.1 未经请求的修改方向：`offer_revision`

LLM 可以主动提出一个具体修改方向，但不能直接输出完整地图。

结构必须是：

```json
{
  "move": "offer_revision",
  "proposalOffer": {
    "summary": "建议方向",
    "rationale": "为什么值得尝试"
  },
  "proposedRows": null
}
```

前端会把它显示成紫色“修改建议”，并在当前 Stage 可编辑时提供“请助手具体生成这个方案”按钮。点击按钮会把明确授权文字填入聊天输入框。

### 8.2 明确授权后的完整提案：`deliver_revision`

只有设计者明确要求生成／修改地图，或明确同意先前方向后，才允许返回完整 rows。

回答和数据结构倾向：

- `move` 必须是 `deliver_revision`；
- `proposedRows` 必须包含完整的 12×10 地图；
- `modificationSummary` 必须只描述真实发生的格子变化；
- 提案必须与当前 Stage 至少有一格不同；
- 后端会再次验证字符、玩家和箱子／目标数量、可解性以及非空差异；
- 验证成功后只形成一个待审查提案，不会立即创建 Stage；
- 只有设计者点击接受后才创建 `llm_accepted` Stage；拒绝不会改动当前 Stage。

模型路由方面，普通对话和开场默认优先使用 `deepseek-v4-flash`；当最新用户消息命中“生成地图、修改地图、map proposal、draft this”等完整地图关键词时，优先使用 `deepseek-v4-pro`。目前关键词检测较窄，因此只回复“好”“同意”不一定会触发 Pro 路由；前端按钮生成的明确授权句能稳定命中“生成一份／map proposal”。

## 9. 最终确认与意图填写

这两个阶段没有 LLM 回答：

1. 设计者点击“确认最终 Stage”后，系统锁定编辑和聊天；
2. 网页显示固定问题：“你希望这个关卡为另一位玩家带来怎样的体验？”；
3. 设计者自行填写最终意图；
4. 系统保存自述意图并完成会话。

LLM 先前产生的 `intentHypothesis` 不会预填、替代或修改最终意图。这样可以避免 LLM 的推测污染设计者的自我报告。

## 10. 模型、超时与失败重试结构

- 普通对话和 Stage 开场首选 `deepseek-v4-flash`；
- 明确完整地图提案首选 `deepseek-v4-pro`；
- 首选模型最多使用 40 秒，另一模型只能使用总计 60 秒内的剩余时间；
- 普通回答最多 1400 tokens，地图提案最多 2400 tokens；
- 温度为 `0.45`；
- thinking 输出被关闭；
- 浏览器在 65 秒进行安全中止；
- 第一次返回无效 JSON 或违反结构时，备用模型只收到安全的结构错误原因，不收到上一份回答内容；
- 同一消息重试复用原 idempotency key，最多保存一个用户 turn 和一个助手 turn。

## 11. 降低“死板感”的当前策略

`cocreation-v15-natural-optional-questions` 针对固定问答节拍做了以下调整：

1. **取消固定写作顺序。** 回答可以是简短直接回应、观察、联想、不同意见或较长反思，不必排列为“回应—评价—提问”。
2. **所有 Stage 都允许不提问。** 包括首次开场、人工编辑开场和恢复 Stage；问题的答案确实会改变下一步时才提问。
3. **保留蓝色问题卡片。** 非空 `followUpQuestion` 仍显示为 `LET'S DISCUSS / 一起聊聊`；为空时整张卡片不出现。
4. **JSON 示例默认无问题。** 系统示例改为 `followUpQuestion: null`，降低模型把提问当成必填项的倾向。
5. **开场段落数放松。** 从固定两至三段改为一至三段，允许一个完整观点独立成答。
6. **归档结构继续保留。** 开场 `assessment`、设计归属、意图隔离、求解证据和提案验证都不因语言放松而取消。

仍可能带来一定模板感的必要边界包括：暂定意图必须使用可纠正的第一／第二人称句式；人工编辑开场需要确认真实修改和可解状态；结构化 JSON、单问题上限和彩色 cue 仍优先保证研究记录与界面可靠性。

调整后的总体形态是：

```text
一个符合当下语境的自然回应
→ 可选的观点、风险或修改方向
→ 只有真正需要设计者输入时才出现蓝色问题卡片
```
