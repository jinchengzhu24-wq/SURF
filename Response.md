# 8010 五张引导卡片

普通对话正文不属于卡片。卡片内容从 `guidance` 中单独渲染，不能与正文重复。

| 卡片 | 触发条件 | 内容结构 |
| --- | --- | --- |
| 绿色 `MANUAL EDIT / 手动编辑` | 直接在右侧编辑器尝试，比继续抽象讨论更有帮助；例如设计者需要直接控制、拒绝当前方向，或尚未说清想修改的局部。提出或存在待接受提案时，也会附带此卡片以提示可自行局部调整。 | `uiCues` 中一项：`{"type":"manual_edit","text":"..."}`。文本说明可尝试的具体局部或编辑方向，不把手动编辑说成必经步骤。 |
| 红色 `WARNING / 警告` | 有充分的确定性或试玩证据支持具体机械风险：明确试玩异常、具体元素间可解释的交互、已验证修改的直接后果，或与设计者已表达方向的明确冲突。普通不确定性、审美偏好和一般取舍不触发。 | `uiCues` 中一项：`{"type":"warning","text":"..."}`。文本以自然的第一人称指出具体游玩时刻、相关元素和风险，不作为求解器事实陈述。 |
| 紫色 `REVISION / 修改建议` | 助手主动给出修改方向，或已有一个等待设计者接受的完整地图提案。未获明确授权时只能给方向，不能生成完整地图。 | 修改方向使用 `proposalOffer`：`{"summary":"...","rationale":"..."}`；前者是短标题式概括，后者说明预期游玩效果和应观察的结果。待接受完整提案则显示该提案的概括。 |
| 橙色 `TENTATIVE INTENT / 暂定意图` | 设计者的表达或操作足以支持对其意图作出推测，尤其是其明确表达第一人称看法、修正或反驳助手判断时。 | `intentHypothesis` 为可纠正的第一、二人称解释，例如“我猜你可能希望……”。内容推断其想达到的可玩效果，不复述操作或写入最终意图。可同时带 `intentConfidence`：`low`、`medium` 或 `high`。 |
| 蓝色 `LET'S DISCUSS / 一起聊聊` | 只有一个具体问题或独立洞见确实能推进当前判断、取舍、行动方向或试玩解读时才出现；不为了填充卡片而生成。 | `followUpQuestion`：一条具体问题，或一条简短的第一人称设计洞见。洞见应指出具体游玩时刻、为何值得观察及其影响的下一项设计决定，且不得重复正文。 |

## 展示规则

- `uiCues` 最多两项，只产生绿色和红色卡片；历史 `tradeoff` 按红色卡片显示。
- 有紫色修改建议或待接受提案时，橙色和蓝色卡片不显示；绿色和红色可作为其辅助卡片显示。
- 有绿色手动编辑卡片、但没有紫色卡片时，橙色、蓝色和红色卡片不显示。
- 其余情况下，橙色、蓝色和红色卡片可按各自触发条件显示。
