# 项目后续研究与技术改进计划

## 一、项目定位与近期目标

项目具有较大的研究潜力，但完整研究与论文写作预计仍需要约两个月。论文目标可从 CHI 转向更适合当前贡献的方向，例如 CSCW、PACM HCI 或 PACMHCI 的其他相关方向，具体投稿 venue 以后续技术贡献和实验结果为依据。

接下来 1–3 周的首要任务是集中改进 RQ1 对应的技术系统：让 AI Agent 更好地理解玩家没有直接说出的设计意图，并协助玩家创作能够表达这些意图的 Sokoban 关卡。

## 二、研究问题与目标

### RQ1：技术系统是否真正有效

需要同时验证以下技术指标：

- 关卡生成成功率；
- 关卡可解性与求解质量；
- 生成关卡对玩家设计意图的匹配程度；
- 生成结果的稳定性、修改幅度和响应时间；
- AI 是否能够解释自己的判断，以及解释是否与实际关卡结构一致。

### 用户体验与交互目标

除技术有效性外，还需要验证玩家是否愿意使用这套系统，以及系统是否支持了真实的共同创作过程：

- 玩家对系统的满意度；
- 玩家对关卡意图的表达能力；
- 玩家对 AI 建议的控制感和信任；
- 玩家是否认为最终关卡仍然体现了自己的设计；
- 玩家对 AI 建议的接受、修改或拒绝行为。

人类不一定会接受 AI 的建议，特别是当玩家为陌生人设计关卡时，玩家可能会质疑或忽略 AI 的判断。这种“人类拒绝 AI 建议”的现象本身具有研究价值，应作为观察和分析对象，而不是简单视为系统失败。

## 三、核心技术路线

单纯依赖 Prompt Engineering 可能不足以形成明确的技术贡献。因此，系统需要引入可验证、可比较的机制。候选方向包括：

- 边界清晰、职责明确的 AI Agent；
- 多智能体协作系统；
- 结构化的长期记忆与短期记忆；
- 用户偏好记忆；
- 独立的关卡评估 Agent；
- 难度 Agent、布局 Agent、意图对齐 Agent；
- 能够解释判断依据，而不仅仅是检测错误的机制。

### 当前落地与后续候选

当前已落地的技术方向是：

> 结构化用户意图与分歧状态 + 双角色语义交接 + 确定性地图验证

独立的 Layout、Difficulty、Evaluator 或 Coordinator Agent 仍是后续研究候选，不属于当前实现，也不能写入 8010 的实际上下文或职责说明。

不必一次性实现上述全部方向。优先选择一至两个与 Sokoban 共同创作最匹配的机制，并围绕它们设计清晰的消融实验和用户实验。

### 当前的系统分工

当前代码不是由多个独立的 Intent、Layout、Difficulty、Evaluator 或 Coordinator Agent 拼接而成。正式职责以 [Multi-Agent/Multi_Agent.md](Multi-Agent/Multi_Agent.md) 为准：8000 使用 `Draft首版理解助手` 和 `关卡蓝图规划助手` 形成并验证首版；8010 使用 `共创聊天助手` 理解意图、分析风险、维护分歧并编译语义契约，使用 `共创关卡修改助手` 在明确授权后生成局部操作候选。候选应用、结构检查和 Sokoban 求解由确定性后端完成。

### 当前的双向质疑流程

```text
用户表达需求 → AI 理解意图 → 意图清楚？
    否 → 普通正文 + TENTATIVE INTENT → 用户澄清 → 重新理解
    是 → AI 分析地图、目标和游玩影响
        ├─ AI REVISION → 风险检查 → 生成 / 质疑 / 换一个方案
        │    ├─ 生成 → execute_revision → 局部候选 → 校验/求解 → 待审查提案
        │    ├─ 质疑 → AI 普通解释 → 用户说明理由
        │    │    ├─ 接受/折中 → 新 REVISION
        │    │    └─ 仍分歧 → LET'S DISCUSS → 继续协商
        │    └─ 换方案 → 不执行原方案 → 不同的 REVISION
        └─ MANUAL EDIT → 用户编辑 → 确定性校验/求解 → human_edit 复核
             ├─ 无具体担忧 → 保留用户修改
             └─ 有具体担忧 → WARNING + LET'S DISCUSS → 协商
```

`WARNING` 只有在具体地图、求解器、试玩或用户明确目标证据支持风险时出现；`LET'S DISCUSS` 只表示尚未解决的具体分歧。分歧没有解决时不产生紫卡；达成用户接受、AI 方向或折中共识后才重新形成紫卡，决定保留当前地图则结束分歧但不创建版本。普通问题留在正文，不以蓝卡代替普通提问。

系统不得把研究者预设的实验目标直接写入 LLM 可见提示。实验条件和研究标签应在系统外部记录；LLM 只能基于玩家实际提供的内容和可观察的关卡证据进行判断。

## 四、实验与评价设计

### 场景覆盖

技术测试需要覆盖约 15–20 个不同情境，至少包括：

- 新手玩家、普通玩家和专家玩家；
- 明确表达的设计意图与较隐含的设计意图；
- 不同的布局关系、推箱依赖和路线节奏；
- 不同难度目标和关卡复杂度；
- 玩家接受、修改和拒绝 AI 建议的情形；
- 玩家为自己设计关卡与为陌生人设计关卡的情形。

每个场景都应保存输入、Agent 中间结果、候选关卡、验证结果、玩家选择和最终关卡，确保可以复现和分析。

### 技术评价指标

- 生成成功率和失败类型；
- 可解率、最优解长度或求解难度；
- 意图对齐评分；
- 玩家修改距离与修改方向；
- AI 建议接受率、拒绝率和二次修改率；
- 多次生成的一致性与候选多样性；
- 平均响应时间与超时比例；
- 解释内容与关卡事实的一致性。

### 用户评价指标

- 满意度；
- 感知控制感；
- 意图表达清晰度；
- 对 AI 的信任与可预测性；
- 对最终关卡作者归属感；
- 对 AI 建议的有用性和可解释性评价；
- 玩家在陌生人关卡设计任务中的接受或抵触行为。

技术指标和用户指标需要同时报告。仅有生成成功或可解性，不能证明系统真正帮助玩家表达了设计意图。

## 五、当前产品与游戏流程改进

除技术系统外，游戏流程仍需要完善：

- 增加并明确 Tutorial PDF 的入口和引导；
- 让玩家先阅读 Tutorial，再点击 Try 进入 Normal 难度；
- 在游戏中补充足够的弹窗、引导和介绍信息；
- 说明玩家、AI Agent 和关卡验证器各自负责什么；
- 让玩家理解 AI 建议可以被修改或拒绝；
- 记录玩家的修改、接受、拒绝和最终选择，供后续研究分析。

这些交互改进应服务于实验理解和数据质量，不应通过额外提示诱导玩家接受 AI 结果。

## 六、近期交付物

导师近期要求的交付内容主要有两份：

### 1. 当前实现报告

需要回答：

- 当前 AI 关卡生成流程是什么；
- 当前系统是否主要依赖 Prompt Engineering；
- 现有生成算法、模板、回退策略和验证器是什么；
- 当前是否有结构化记忆、用户偏好记忆或反馈机制；
- 当前如何检查关卡可解性、难度和意图匹配；
- 当前系统有哪些失败案例和已知限制。

### 2. 技术改进方案

需要结合近期 CHI/CSCW 相关工作，说明：

- 准备引入哪些技术；
- 每项技术为什么适合 Sokoban 共同创作；
- 技术如何构成项目的原创贡献；
- 技术如何支持 RQ1；
- 如何通过基线、消融实验和用户实验验证改进是否有效。

## 七、分阶段执行计划

### 第 1 阶段：梳理现状与确定贡献边界

- 完成当前实现报告；
- 绘制现有系统的数据流和 Agent/模块职责；
- 列出当前生成、验证、记忆和反馈机制；
- 收集失败案例，按生成失败、不可解、意图不匹配、解释不一致和交互问题分类；
- 从“结构化用户意图与分歧状态”“双角色语义交接”和“确定性验证”中确定主要贡献及可选扩展。

### 第 2 阶段：实现 RQ1 技术原型

- 定义结构化意图表示、置信度和可修正字段；
- 明确共创聊天助手与共创关卡修改助手的职责、输入边界和交接契约；
- 建立候选关卡生成、确定性验证和评价记录流程；
- 为每个候选关卡保存生成依据、验证结果和意图对齐结果；
- 增加可解释的反馈，使玩家能够理解建议、修改或拒绝建议；
- 确保未经玩家明确授权的建议不会直接成为最终关卡。

### 第 3 阶段：技术测试与消融实验

- 准备 15–20 个覆盖不同玩家、意图和复杂度的测试场景；
- 对比现有 Prompt-only 基线与新技术方案；
- 分别移除结构化记忆、独立评估 Agent 或多 Agent 协调机制，观察性能变化；
- 记录生成成功率、可解率、意图对齐、延迟和解释质量；
- 修复高频失败类型，并冻结一版用于用户实验的系统。

### 第 4 阶段：用户研究与论文材料整理

- 设计玩家接受、修改和拒绝 AI 建议的任务；
- 比较不同熟练度玩家和不同设计关系下的体验；
- 收集满意度、控制感、信任、意图表达和作者归属感；
- 分析人类拒绝 AI 建议的原因及其对共同创作的影响；
- 整理技术贡献、设计启示、限制和后续工作；
- 根据贡献和实验结果确定最终投稿方向。

## 八、待确认事项

以下内容暂不提前固化，应在系统原型和初步实验后确认：

- 最终投稿方向是 CSCW、PACM HCI 还是其他 venue；
- 是否将 PC/DG 作为正式研究条件；
- 最终实验包含多少共同创作轮次；
- 问卷指标和最终研究问题的具体措辞；
- 主要贡献是结构化记忆、多智能体协作，还是两者结合；
- 是否加入独立的难度、布局或意图对齐 Agent；
- 是否需要进一步扩展用户偏好记忆或长期个性化能力。

## 九、完成标准

本计划阶段性完成的最低标准是：

1. 能够清楚说明当前系统如何生成、验证和保存 Sokoban 关卡；
2. 至少实现一项超越 Prompt Engineering 的、职责明确且可验证的技术机制；
3. 完成 15–20 个不同情境的技术测试；
4. 同时获得技术效果和用户体验两类证据；
5. 能够解释玩家接受或拒绝 AI 建议的原因；
6. 形成一份能够支撑论文技术贡献和实验设计的完整方案。

## 十、按简易程度划分的执行事项

以下顺序按照“先完成容易、低风险且能产生后续基础的事项，再处理复杂技术”的原则排列。这里的优先级是执行优先级，不代表研究价值高低。

### P0：最容易，立即完成

目标：快速明确范围，形成可检查的基础材料。

1. **冻结 RQ1 的工作表述**
   - 明确“理解隐含意图”和“生成表达意图的关卡”两个核心对象；
   - 暂时不扩展到完整的个性化、长期记忆或复杂多智能体系统。

2. **完成当前实现报告**
   - 记录当前 Prompt、生成流程、回退逻辑、LevelSolver 和已有反馈机制；
   - 列出当前系统已经能做什么、不能做什么，以及典型失败案例。

3. **绘制现有系统流程图**
   - 标出玩家输入、LLM、地图生成器、验证器、Unity 和 8010 服务之间的数据流；
   - 标注哪些内容是临时草稿、哪些内容会保存为 Stage 或研究记录。

4. **建立 15–20 个测试场景表**
   - 先只写场景、玩家类型、意图类型、难度和预期结果；
   - 同时覆盖新手、普通玩家、专家玩家，以及接受、修改和拒绝 AI 建议的情况。

5. **确定评价指标和记录字段**
   - 技术字段：生成成功、可解性、意图匹配、延迟、失败原因；
   - 交互字段：接受、拒绝、修改、满意度、控制感和信任；
   - 确认每次生成、验证和玩家决策都可以追溯。

**P0 产出：**当前实现报告、系统流程图、测试场景表、评价指标表。

### P1：较容易，1–3 天内完成

目标：在不大幅改变现有系统的情况下，补齐可验证性。

6. **整理失败案例分类**
   - 生成失败；
   - 地图不可解；
   - 意图匹配不足；
   - 难度或布局不符合预期；
   - AI 解释与地图事实不一致；
   - 玩家不知道如何接受、修改或拒绝建议。

7. **定义结构化用户意图格式**
   - 保存意图内容、来源、置信度、证据和是否得到玩家确认；
   - 区分“玩家明确说出的意图”和“AI 推测的意图”；
   - 为意图保留修正和覆盖入口。

8. **补充确定性验证和日志**
   - 统一记录 LevelSolver 结果、地图统计、生成耗时和失败原因；
   - 让 LLM 负责解释和建议，让确定性程序负责可解性和格式判断；
   - 确保无效候选不会直接成为最终 Stage。

9. **完善最小用户反馈闭环**
   - 玩家可以明确接受、修改或拒绝 AI 建议；
   - 保存玩家的决定及其前后地图差异；
   - 不把 AI 的推测自动写成玩家的最终意图。

**P1 产出：**意图数据结构、失败分类表、验证日志、玩家决策记录。

### P2：中等难度，3–7 天完成

目标：实现第一个可以与 Prompt-only 基线比较的技术原型。

10. **完善共创聊天助手的意图与分歧状态**
    - 区分用户明确表达、AI 暂定理解和双方已达成的共识；
    - 对明确方向形成概念 `REVISION`，对不清方向只形成 `TENTATIVE INTENT`；
    - 只有具体证据支持时才进入 `WARNING + LET'S DISCUSS`，不把普通问题当成分歧。

11. **完善共创关卡修改助手的确定性交接**
    - 只接收明确授权的 `RevisionPlan` 和 `executionContract`；
    - 不接收完整对话、8000 `dgContext`、研究者目标或未确认意图；
    - 让后端原子执行、结构校验、求解并把结果作为待审查提案交给用户。

12. **增加候选地图比较流程**
    - 生成多个候选地图；
    - 先通过确定性验证，再按意图匹配和目标指标排序；
    - 保存被选中、被拒绝和被淘汰的候选及原因。

13. **完成基线对比**
    - 对比当前双角色交接流程与 Prompt-only 流程；
    - 使用同一批 15–20 个场景；
    - 比较生成成功率、可解率、意图匹配、延迟和解释质量。

**P2 产出：**第一个技术原型、基线实验结果、候选地图记录和失败分析。

### P3：较复杂，需要实验依据后再做

目标：在 P2 结果证明有必要时，扩展系统的研究贡献。

14. **评估是否需要更多独立 Agent**
    - 只有当基线结果显示现有聊天助手的布局或难度判断是主要瓶颈时才考虑；
    - 通过消融实验验证新增 Agent 是否带来实际改进；
    - 不为了增加 Agent 数量而增加系统复杂度，也不改变当前两角色交接边界。

16. **扩展短期、长期和用户偏好记忆**
    - 短期记忆保存当前轮次的对话和草稿；
    - 长期记忆保存跨版本的设计决策和反馈；
    - 用户偏好记忆只保存经过授权且可解释的偏好；
    - 分析记忆是否真的提升了意图匹配，而不是只增加上下文长度。

17. **开展完整消融实验和用户研究**
    - 移除结构化意图记忆、风险协商或双角色交接机制；
    - 比较不同熟练度玩家和陌生人设计情境；
    - 重点分析玩家为什么接受、修改或拒绝 AI 建议。

18. **根据结果决定论文投稿方向**
    - 先根据技术贡献和用户研究结果判断更适合 CSCW、PACM HCI 或其他 venue；
    - 不在技术贡献尚未稳定前提前固定投稿方向。

**P3 产出：**可选 Agent/记忆机制实验、消融结果、用户研究数据和论文贡献总结。

### 建议执行顺序

```text
P0 现状梳理与指标确定
 ↓
P1 数据结构、验证日志与玩家决策闭环
 ↓
P2 双角色交接 + 意图/分歧状态 + 基线对比
 ↓
P3 可选 Agent/记忆扩展与完整用户研究
```

如果时间有限，至少完成 P0–P2。P0–P1 能够支撑当前实现报告，P2 才能形成相对清晰的技术改进证据；P3 属于根据前期结果决定是否扩展的研究方向。
