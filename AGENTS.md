# Repository Guidelines

## Project Structure & Module Organization

This repository combines a Unity 2D Sokoban client with a Python service. Unity gameplay code lives in `Assets/Scripts/`, grouped by feature (`Level/`, `LLM/`, `Player/`, `Study/`, and `Menu/`). Scenes are under `Assets/Scenes/`; prefabs, tiles, animations, fonts, and WebGL templates remain in their corresponding `Assets/` folders. Keep every Unity asset paired with its `.meta` file.

`Backend/` contains the FastAPI application, LLM runtime and prompts, plus Python tests. `Frontend/` is the static study dashboard served at `/frontend/`. Unity package and editor settings live in `Packages/` and `ProjectSettings/`. Treat `Library/`, `Temp/`, `Logs/`, generated solution files, and `WebGLBuild/` as generated output.

## Current Project Status (2026-08-11)

### Supervisor feedback direction (2026-08-03)

`Assets/EssayBase/8-3/SURF_Feedback.pdf` is the source record, and `Assets/EssayBase/8-3/Feedback_Action_Plan.md` is the working interpretation and implementation plan. The next prototype should shift the research focus from researcher-defined, one-shot Competitive/Supportive generation to a persistent, multi-turn human-LLM co-creation loop around the same evolving level.

- Keep any designer goal or experimental condition separate from the LLM-visible prompt. Do not automatically inject researcher-authored Competitive/Supportive definitions unless a later confirmed study design explicitly requires it.
- Do not end a co-creation session after the first valid level. The LLM must assess the current version, ask whether it is good enough, and support repeated feedback and revisions grounded in the actual current level and relevant conversation history.
- Continue using deterministic validation, the solver, and gameplay evidence. Treat LLM solution or difficulty commentary as participant-facing opinion, not ground truth.
- Preserve every chat turn, level version, modification, user accept/reject/revise decision, timing event, final level, and opponent outcome for later analysis.
- Require explicit designer confirmation before sending a level to the opponent, then collect separate designer and opponent feedback that can be joined to the same match/design session.
- Reuse the existing two-player, challenge-exchange, opponent-play, result, and questionnaire infrastructure where possible. The legacy Competitive/Supportive route and PC/DG topology behavior were explicitly retired on 2026-08-11; do not restore them without a newly confirmed study design.
- The replacement co-creation path must begin with a neutral design brief and must not assign the designer a predefined Competitive, Supportive, difficult, friendly, or similar goal. Let the designer form and express an intention naturally. Record the designer's self-reported intention only after final level confirmation and before opponent results can influence that report.
- Human-first versus LLM-first, any non-goal study condition, minimum round count, final questionnaires, measures, and research questions remain undecided. Do not encode them as fixed requirements before confirmation.

### Plan conflict guardrail

- Treat `Assets/EssayBase/8-3/SURF_Feedback.pdf`, `Assets/EssayBase/8-3/Feedback_Action_Plan.md`, and the confirmed future-route description in `README.md` as the current planning baseline.
- If a later user suggestion or requested implementation conflicts with that baseline, do not implement it immediately. First identify the specific conflict, explain which confirmed direction it would violate, and stop before making related changes.
- Resume only after the user explicitly confirms that the established plan should be revised or replaced. When that happens, update the relevant planning documentation before or together with the implementation so the repository does not retain contradictory instructions.
- Do not treat an idea as conflicting merely because it is new, more detailed, or covers an undecided item. Compatible extensions may proceed normally; unresolved research decisions still require confirmation before being encoded as fixed behavior.

The active matchmaking route is:

```text
Menu -> Online_Lobby -> Match_Briefing -> Draft
                                          -> PC -> PC_Design -> PC_Level
                                          -> DG -> DG_Level
     -> CoCreation_Entry -> 8010 Stage loop -> final confirmation -> intention
     -> confirmed rows return to Unity -> Challenge_Waiting
     -> Online_Level -> Match_Result -> Questionnaire(Online) -> Menu
```

`Competition_Mode` and `AI_Asistant_Mode` have been removed; the initial-method chooser is `Draft`. The PC branch is separate from DG and Creative Workshop data. `PC_Level` reads only the sketch saved by `PC_Design`. `CoCreation_Entry` is enabled in Build Settings and is the bridge between a verified Unity first draft and the persistent 8010 co-creation session.

### PC_Design

- The editable sketch is fixed at `12x10` and uses `#` for user walls, `s` for box starts, `t` for targets, and spaces for editable cells.
- A valid submission contains one or two matching `s`/`t` pairs, one enclosed connected activity area of at least 56 cells, and no `s` orthogonally adjacent to a wall.
- Check/Submit also requires room for at least one `2x2` water area and three generated internal wall cells. No water tile may have a wall directly above it. Generated wall cells must be at least one orthogonal cell away from the outer shell; diagonal contact is allowed.
- `PCDesignContext` persists the submitted sketch so Back from `PC_Level` restores it.
- Relevant files are `Assets/Scripts/Level/PCLevelSketchController.cs`, `PCDesignFeasibilityValidator.cs`, and `PCDesignContext.cs`.

### PC_Level generation

- Unity calls `POST /generate-pc-level` at `http://111.231.136.4:8000/generate-pc-level`. The serialized scene and client timeout are both 30 seconds.
- The request includes `width`, `height`, `sketchRows`, optional retry context, and `maxAttempts`. The response remains `{"rows":[...10 rows...]}`. A legacy client may still send `competitionMode`; Pydantic ignores it and the backend must not store or apply it.
- The backend normalizes the request once, enumerates legal `2x2` through `4x4` water rectangles, and checks at most six water choices.
- For each checked water choice, the backend solves the no-new-wall map once and records a complete solution trace. Generated walls are selected only from cells never occupied by the player or boxes in that trace, so the recorded solution remains valid after the walls are added.
- Cheap greedy scoring constructs at most six complete layouts. Five internal walls are preferred, four are retained as an intermediate fallback, and the guaranteed minimum is three internal walls plus one water area. Bent, split, or dispersed wall shapes rank above a straight line.
- Generated walls may not cover fixed user tiles, touch a box start, divide the activity area, sit orthogonally next to an outer-shell wall, appear directly above water, or contain a complete `2x2` block made only from generated wall cells. User-authored wall blocks are not subject to the generated-wall `2x2` restriction. There is currently no 48-cell post-generation activity-area minimum; the PC_Design submission minimum remains 56.
- PC generation is mode-neutral. It does not require generated wall groups to be dispersed or fully connected; the general safety, activity-area connectivity, generated-wall `2x2`, water-clearance, and solvability rules remain authoritative.
- The LLM receives complete layouts and returns only `{"layoutCandidateId": n}`. It cannot mix player, water, or wall IDs from different candidates.
- PC generation makes one model call with a 15-second backend timeout. Invalid JSON, an unknown ID, timeout, connection failure, or other `LLMServiceError` immediately selects the highest-ranked safe candidate instead of making a second model call.
- Do not restore the deleted wall-combination enumeration, counterfactual wall-impact search, or the requirement that walls increase shortest steps or pushes. Stability and guaranteed solvability are the current priority.
- Unity still validates the full rows and runs `LevelSolver` with `maxSearchStates: 300000` before loading. Its minimum generated feature check is three new wall tiles and one rectangular water area.
- Relevant files are `Backend/app.py` (`normalize_pc_level_request`, `build_pc_safe_layout_candidates`, and `create_pc_level_candidate`), `Backend/prompt.py`, `Assets/Scripts/LLM/PCLevelExpansionClient.cs`, `Assets/Scripts/Level/PCLevelExpansionController.cs`, and `PCLevelCandidateValidator.cs`.

### DG_Level generation

- `LLMLevelDesignClient` no longer sends a researcher-defined mode while generating a DG level plan.
- `LevelGenerator` uses its general wall templates and corridor behavior in `DG_Level`; there is no hidden Competitive/Supportive topology branch.

### Persistent co-creation and Stage play

- In normal generation mode, a successfully verified `PC_Level` or `DG_Level` is not played or submitted immediately. Its exact rows and initial method are staged in `CoCreationDraftContext`, then Unity loads `CoCreation_Entry`.
- `CoCreation_Entry` creates the 8010 session idempotently, opens its launch URL, polls the integration endpoint, and only stages the confirmed final rows for `Challenge_Waiting` after the designer has submitted an intention.
- The 8010 service stores sessions, immutable level versions, turns, LLM assessments, proposals, decisions, play attempts, intentions, and audit events in its own SQLite/WAL database. Never place rows in a Play URL or expose a browser/session token to another Stage.
- The 8010 frontend follows the active 8000 Pixel-adventure dashboard theme: grass-grid background, wooden topbar, stone panels, blue/purple title plates, hard pixel shadows, and monospace display text. All visible text uses a minimum 700 weight, controls/status labels use 800, and the main title remains 900. It keeps its own responsive Stage/chat/map layout and independent CSS.
- The 8010 LLM is an adaptive, conversational design peer rather than a fixed assessment form. Its highest-priority voice is rational, warm, friendly, and predominantly first-person; it must vary openings and paragraph rhythm, avoid mechanical paraphrase, and never fall into a fixed acknowledgement-evaluation-question template. Only the very first Stage 1 opening is questionless: it selects one or two concrete authored choices, offers a clearly subjective perspective, and naturally mentions that the designer may share an impression, play the Stage, or make a local edit. Every later turn—including later conversation while still on Stage 1—may use a concrete question or an independent LLM insight when that advances an unclear evaluation, meaningful trade-off, actionable direction, or new play evidence. The blue `LET'S DISCUSS / 一起聊聊` card may contain either kind of focus; it is not restricted to questions. A stated preference may lead to a deeper question but must not be re-asked for approval, and consecutive cards must advance different judgments. Do not use workflow greetings, inventory the map, or ask for an overall player-experience category. A Stage created by accepting an LLM proposal must not receive another first assessment: the original assistant turn that generated the accepted proposal becomes that new Stage's carried opening, and later dialogue continues from it. Any inferred intention must remain tentative and correctable and must never populate the designer's final intention.
- A co-creation session contains exactly one Sokoban level. `Stage 1`, `Stage 2`, and later Stage numbers are immutable saved versions of that same level, never separate levels, campaign order, or difficulty progression. Model prose, guidance cards, archival assessment fields, proposal summaries, and translations are normalized before persistence so phrases such as `第二关`, `下一关`, `后面关卡`, `second level`, and `later levels` cannot present versions as multiple levels.
- Natural authorization such as `你帮我改`, `你来改吧`, `按这个思路改`, `Can you change it?`, or `Go ahead and revise it` must enter the strict complete-map proposal workflow when prior conversation supplies the direction. Questions such as `你觉得怎么改` or `How would you change it?` remain ordinary discussion. Ordinary chat must never claim `改好了`, say a map was changed, or invite play before `proposedRows` exist; even a validated proposal is pending until the designer explicitly accepts it. A generic execution request is not itself a design intention and must not be echoed into the orange intent card.
- Ordinary design replies should normally have two to four compact paragraphs, with enough concrete observation, first-person interpretation, reasoning, and playable illustration to feel human without becoming a report. Very simple factual replies may remain shorter. Guidance cards should carry complete natural thoughts rather than slogans, and proposal rationales should explain the expected play effect.
- Guidance cards must distill or expand the exchange rather than copy visible prose. Purple proposal cards use a short title-like synthesis plus an independent explanation of the expected playable effect and what to judge. Blue discussion cards turn an extracted question or insight into a standalone focus with a concrete play moment and the next design decision it informs. If the model copies a body sentence into a card, deterministic normalization replaces or suppresses it. When a complete-map proposal is structurally invalid, the second corrective attempt stays on Pro with the safe validation reason; Flash remains the fallback for an empty, timeout, or connection failure. Empty-response UI copy must not claim both models were blank when an earlier attempt was instead invalid.
- Every `intentHypothesis` must speak directly from the assistant to the player. English uses tentative first/second-person forms such as `I think you may...`, `My guess is that you might...`, or `I get the sense that you may...`; Chinese uses forms such as `我猜你可能……` or `我感觉你似乎……`. Report-style third-person forms such as `the designer/player wants...` and `设计者/玩家想要……` are forbidden and deterministically normalized before persistence and rendering.
- LLM attribution must follow both `initialDraftMethod` and the saved Stage source on every turn. For a DG (`description_generation`) initial Stage, the designer controlled only the upstream description and parameters while the generator placed every exact tile; do not ask why the designer placed a wall, water, box, target, player, or corridor, and do not invent parameter values that 8010 does not receive. For a PC (`partial_completion`) initial Stage, the designer controlled box starts, targets, and broad sketch/room constraints, while completion added exact water, generated internal walls, the player start, and remaining layout; because final rows do not preserve per-wall provenance, never attribute a particular internal wall to the designer. A `human_edit` Stage may attribute only deterministically detected changed components to the designer; `llm_accepted` is an accepted shared direction and `restored` is a revisited historical version.
- An unsolicited revision idea may contain only a direction and rationale. A complete map proposal is allowed only after the designer explicitly requests or agrees to it, and still creates a Stage only after deterministic validation and explicit acceptance. Every complete proposal must differ from its base Stage by at least one actual tile; prompt instructions require claims to match the before/after rows, and the backend independently rejects an empty diff both before saving a proposal and again before accepting it. Assistant guidance metadata is persisted separately from natural chat text.
- Complete-map proposal generation uses one to three internal operation candidates rather than asking the model to reproduce all 120 cells. Each candidate may contain at most 24 one-based tile operations. The backend verifies every coordinate and before/after tile, rejects duplicate, no-op, void, connected outer-shell, or out-of-scope changes, applies each candidate to the exact current rows, and runs the existing deterministic proposal validator and solver. Only the first safe candidate becomes the visible pending proposal. Proposal-card distillation must stay within the selected proposal's own summary, rationale, and actual diff; it must never borrow an unrelated direction from earlier conversation.
- Relaxed proposal generation is a high-threshold recovery path, not a normal fallback. It may be offered only after three failed requests for the same Stage, message idempotency key, and normalized revision brief, where every request ended with `MODEL_RESPONSE_INVALID` after at least two model attempts. Empty output, timeout, rate limit, connection, configuration, and server failures never count. At the threshold, persist an honest assistant turn with a red warning cue and an `awaiting_confirmation` relaxation offer, but no rows or purple proposal cue. The only relaxation is that the proposal may realize one coherent local, play-testable effect instead of every requested secondary effect at once; the designer's core direction, solvability, outer shell, explicit prohibitions, and unrelated areas remain fixed. Generate a relaxed proposal only after the designer's next message explicitly agrees. A rejection, unrelated reply, intervening assistant turn, or failed relaxed proposal must not silently authorize or recursively offer another relaxation.
- The LLM may briefly point out the right-side tile editor when direct experimentation would help the designer, but must not repeat the hint mechanically or frame manual editing as required. After a verified `human_edit` Stage is saved, the backend deterministically classifies changes to the outer shell, water, internal walls, boxes, targets, player, and floor area relative to its parent. The assistant opening must acknowledge the actual changed components and verified solvability before evaluating the design effect and continuing the dialogue.
- Assistant guidance uses structured, accessible UI cues inside the same message bubble: manual editor hints are green, optional questions are blue, revision directions/proposals are purple, tentative intention hypotheses are orange, and strongly evidence-grounded risks are red. Each cue is bold and carries a bilingual text label. A non-null `followUpQuestion` continues to render as the blue `LET'S DISCUSS / 一起聊聊` card, but the card is absent when no question is useful. Warning cards require an explicit play anomaly, a mechanically explainable interaction between concrete map elements, a verified change with a direct consequence, or a clear conflict with the designer's stated direction; ordinary uncertainty and aesthetic or strategic trade-offs remain in prose. Warning and intention cards use varied, natural first-person language rather than stock alert or report phrasing. New `manual_edit` and `warning` cues live in the optional `guidance.uiCues` array; historical `tradeoff` cues remain compatible and render as warnings, while historical turns are never keyword-classified.
- Conversation is Stage-scoped without deleting research records. Every turn retains its `version_id`; the frontend shows only turns relevant to the selected Stage, historical Stage chat is read-only, and `build_llm_context` supplies only relevant Stage turns. The accepted proposal's assistant turn is linked into the resulting Stage through the existing proposal/decision/version relationship rather than copied or reassigned, so the research record remains intact. Composer drafts and pending message recovery belong only to the current Stage.
- Each 8010 business request has a 60-second wall-clock hard limit. Ordinary chat and Stage openings prefer `deepseek-v4-flash`; explicit complete-map proposals prefer `deepseek-v4-pro`. The preferred model gets at most 40 seconds, then the other model may use only the remaining total time. DeepSeek thinking output is explicitly disabled so reasoning tokens cannot consume the whole visible JSON budget. One question embedded in `assistantMessage` is extracted at sentence level into `followUpQuestion` while declarative content is preserved; an existing structured question wins over a duplicate body question. Pure-question and multi-question bodies remain invalid. `followUpQuestion` and the archival opening `satisfactionQuestion` may both be null, including for Stage openings; when a question exists, Stage openings must place it in structured output. After a structurally invalid first attempt, the fallback receives only a safe validation reason (never response/chat content) appended to the system contract and must return a fresh corrected JSON object. `llm_attempt_failed` logs that safe `validationReason` for diagnosis. Both attempts share one request and idempotency key, and only the successful result is persisted. The browser displays elapsed time and applies a 65-second safety abort. Timeout, refresh, or connection recovery must reuse the original key so one logical request has at most one user turn and one assistant turn. Reusing a key with different content or a different Stage is an idempotency conflict.
- Web Play uses a five-minute single-use ticket. `CoCreationPlayBootstrap` exchanges it once, clears it from the browser URL, and loads `PC_Level` for `partial_completion` or `DG_Level` for `description_generation`.
- While `CoCreationPlayContext` is active, PC/DG generation must remain disabled. The Stage rows are revalidated with Unity `LevelSolver`; invalid or expired input must never fall back to random generation.
- Stage Play records cumulative moves, pushes, restarts, and active time. After the normal completion animation and fade, it submits the completed metrics and automatically returns the current tab to the same 8010 session and Stage. There is no in-game early-return control; browser back, close, or refresh remains an interrupted attempt. Stage Play must not create a version, submit a challenge, or enter results/questionnaire routes.

### Online matchmaking and challenge exchange

- `MenuController.OpenMatchmaking()` loads `Online_Lobby`. Players create or join an anonymous two-player room, enter `Match_Briefing`, and move directly to `Draft` after both Ready flags are true.
- The backend uses a single-process in-memory room store with six-character room codes, anonymous player tokens, one-second HTTP polling, lazy 30-minute expiry, and the states `waiting_for_opponent`, `briefing`, `waiting_for_challenges`, `challenges_ready`, `waiting_for_results`, `results_ready`, and `cancelled`.
- Room endpoints are `POST /online/rooms`, `POST /online/rooms/join`, `GET /online/rooms/{matchId}`, and the authenticated `/ready`, `/challenge`, `/result`, and `/leave` endpoints. Authenticated requests use `X-Player-Token`.
- Accepted room state changes are also appended to `Backend/study_logs/online_match_events.jsonl` for the MatchMaking dashboard. New events store room/player numbers, `aiAssistantMode`, full challenge rows, results, and server timestamps, but never player tokens or `competitionMode`. GET polling and identical retries do not add events. Historical JSONL lines remain unchanged, while derived dashboard data omits the retired field.
- `OnlineMatchContext` is runtime-only. It stores the room identity, pending own rows and AI assistant method metadata, opponent rows, room state, and results. Before entering `Questionnaire(Online)`, it temporarily preserves `matchId`, room code, and player number until the survey succeeds so the response can be joined to the match. Refreshing WebGL or restarting the server does not restore a match.
- In an online room, only the final rows returned after 8010 final confirmation and intention are staged for `Challenge_Waiting`. Initial PC/DG generation and optional Stage Play must never submit a challenge.
- Challenge submissions include `rows` and `aiAssistantMode`. Identical repeats are idempotent; changed rows or method metadata are rejected after the first accepted submission. A legacy extra `competitionMode` is ignored and never stored or returned. The server returns only the opponent rows to each authenticated player after both submissions.
- `Online_Level` validates and solves the opponent rows locally before loading them. It uses `Player2` with arrow keys; PC/DG continue to use `Player` with WASD. Time and successful moves accumulate from first control until solve and are not reset by `R`.
- Results include `durationSeconds`, `moveCount`, and `minimumMoves`. After `Online_Level` submits the local result successfully, its completion panel enables the `LEAVE` button and waits for the player to click it before loading `Match_Result`; this transition does not leave the room or clear `OnlineMatchContext`. `Match_Result` displays each challenge's AI assistant method and both runs and polls while one result is missing. Continue leaves the room, clears `OnlineMatchContext`, and loads `Questionnaire(Online)`.
- `Questionnaire(Online)` reuses `QuestionnaireController` with survey ID `online_post_match_survey`, does not require another nickname, and targets `Menu`. It uses three discrete 1-to-5 sliders with visible integer ticks, circular handles, live score boxes, and a valid default score of 3. Scores remain compatible with the existing answer envelope through `optionIndex`, `score_N` in `optionId`, and the numeric `optionText`. It must load Menu only after `/record-survey-response` succeeds; a failed submission remains on the questionnaire for retry.
- `/frontend/` is the MatchMaking dashboard and `/frontend/train.html` preserves the original Train dashboard. Both share the existing visual system and provide a top-level flow switch. MatchMaking records are exposed through `/matchmaking-records-data`; password-protected deletion uses `/delete-online-match` and `/clear-matchmaking-records` without changing Train records.
- `ProjectSettings/ProjectSettings.asset` currently has `runInBackground: 0`. A background WebGL tab may pause polling; dual-browser tests must refocus the waiting result page and allow another polling interval before treating missing data as a role-mapping bug.
- Online scene UI is serialized and statically visible in the editor. Relevant files are under `Assets/Scenes/Matchmaking/Online/`, `Assets/Scripts/Online/`, and `Assets/Scripts/Study/QuestionnaireController.cs`. `CoCreation_Entry` and the existing online scenes are enabled in `ProjectSettings/EditorBuildSettings.asset`.

### Deployment and verification state

- Remote verification on 2026-08-11: the neutral 8000 backend, dashboard, and rebuilt WebGL were deployed; `/health`, `/ready`, `/frontend/`, `/game/`, and all four WebGL build assets returned HTTP 200. The WebGL cache key is `cocreation-20260811-3` and includes `CoCreation_Entry`, the PC/DG dual-mode Stage Play bootstrap, automatic return to the originating 8010 session after completion synchronization, no Stage Play return overlay, and the online questionnaire route.
- The independent 8010 FastAPI service, persistent SQLite/WAL schema, and three-column bilingual frontend were deployed on 2026-08-11. The 60-second wall-clock hard limit, 40-second preferred-model window, Flash/Pro fallback, message-idempotency recovery, elapsed-time UI, larger map-panel text, editor-aware guidance, verified human-edit acknowledgement, Stage-scoped conversation, accepted-proposal Stage opening, global bold typography, and five-color guidance cues were deployed on 2026-08-12 with frontend cache keys `cocreation-pixel-20260812-5` and `cocreation-guided-20260812-9`. Thinking-disabled structured generation, sentence-level question normalization, validation-aware fallback, and non-empty proposal enforcement were deployed later that day. The deployed workbench uses adaptive prompt `cocreation-v14-nonempty-proposals`, keeps complete solver move traces out of LLM context, distinguishes PC/DG and later Stage authorship on every turn, normalizes intention hypotheses into direct first/second-person speech, and labels blue questions as `LET'S DISCUSS / 一起聊聊`; an old first-assessment turn already recorded for an accepted-proposal Stage remains in SQLite for research integrity but is excluded from that Stage's UI and future LLM context. Existing databases gain nullable `conversation_turns.guidance_json` without rewriting old turns. Its systemd service reads a separate protected `.env`, reports `tokenSecretConfigured: true`, and stores data at `/root/SURF/CoCreationPrototype/Backend/data/cocreation.sqlite3`.
- A stale CPU-bound backend process from the former exhaustive search was force-terminated. Before killing any process in future work, identify the exact stale PID with `ps`; never broadly kill Python processes.
- For an 8010-only deployment, copy only the changed `CoCreationPrototype` files and run `systemctl restart sokoban-cocreation`; never run `/root/SURF/deploy_scp`, because that script manages the 8000 backend and can leave a standalone 8000 process conflicting with `sokoban-backend`. The local `deploy_scp.ps1` uploads a broader set including WebGL and frontend assets, so do not use it for an 8010-only change.
- Deployment verification on 2026-08-13: prompt `cocreation-v15-natural-optional-questions` removes the fixed acknowledgement/evaluation/question rhythm, allows `followUpQuestion` and the archival `satisfactionQuestion` to be null for every Stage opening and later turn, and keeps non-null questions in the existing blue `LET'S DISCUSS / 一起聊聊` card. Stage openings now allow one to three short paragraphs. The 8010 frontend app cache key is `cocreation-guided-20260813-10`. The independent 8010 suite passes 70 tests locally and on the server, `node --check CoCreationPrototype/Frontend/app.js` passes, `/`, `/health`, and `/ready` return HTTP 200, and `tokenSecretConfigured` remains true. The SQLite database passed `PRAGMA integrity_check`; its size and modification time were unchanged by deployment. A pre-deployment copy is stored at `/root/SURF/backups/cocreation/cocreation-20260813-140321.sqlite3` and the prior runtime/WebGL files are under `/root/SURF/backups/deploy-20260813-141100-retry`.
- Deployment verification later on 2026-08-13: prompt `cocreation-v19-natural-peer-dialogue` makes Stage 1 questionless with natural chat/play/edit orientation, asks concrete questions at later decision points, permits deeper questions after a stated direction, varies first-person intention wording, and raises warning cards only from strong mechanical or play evidence. The server suite passes 108 tests. A non-persistent Chinese probe confirmed a questionless Stage 1 opening, specific blue questions after both an evaluation and a stated direction, no warning card in either ordinary turn, first-person prose, and unchanged database counts. Only `sokoban-cocreation` was restarted; the 8000 backend retained its PID and start time. The pre-deployment database and runtime are under `/root/SURF/backups/natural-peer-v19-20260813-1755`.
- Deployment verification on 2026-08-13 for prompt `cocreation-v20-single-level-dialogue`: the system now treats the session as exactly one level with immutable Stage versions and normalizes false progression phrases before persistence and translation. Only the first Stage 1 opening is questionless; all later turns, including later turns on Stage 1, may place either a concrete question or a concise first-person design insight in the blue discussion card. The local and server suites pass 114 tests, Python compilation and frontend JavaScript checks pass, and non-persistent Chinese probes left database counts at 38 sessions, 112 turns, and 48 versions. During this deployment `/root/SURF/deploy_scp` was found to restart 8000 rather than 8010 and temporarily left a standalone process conflicting with systemd; the exact stray PID was removed, `sokoban-backend` was restored to one healthy systemd-managed process, and the deployment rule above was corrected. The v20 database and runtime backup is `/root/SURF/backups/single-level-v20-20260813-1827`.
- Deployment verification on 2026-08-13 for prompt `cocreation-v21-lively-explicit-revisions`: natural Chinese and English requests to let the assistant make the discussed change now use the Pro complete-map proposal path, while hypothetical design questions stay in ordinary chat. Replies target two to four compact conversational paragraphs and 900 visible tokens. Pending-state normalization prevents ordinary replies and proposals from presenting an edit as already saved. Local validation passes 122 tests, Python compilation, frontend JavaScript syntax, and diff checks; the server passes the same 122 tests. A non-persistent Chinese probe produced a solver-validated, non-empty proposal through Pro in about 5.6 seconds and left the database unchanged at 39 sessions, 123 turns, 49 versions, and 9 proposals. Only `sokoban-cocreation` was restarted; `sokoban-backend` retained PID 3866500 and its 18:28:58 start time. The v21 pre-deployment backup is `/root/SURF/backups/lively-revisions-v21-20260813-1856`.
- Deployment verification on 2026-08-13 for prompt `cocreation-v22-distilled-guidance-reliable-proposals`: purple proposal cards are distilled into a short design-move title plus an independent playable rationale, while blue cards refine or expand questions and first-person insights into standalone decision-focused discussion. Structurally invalid complete proposals retry on Pro; empty/timeout/connection failures still use Flash. The frontend error now reports the final blank attempt accurately, and its script cache key is `cocreation-distilled-guidance-20260813-14`. Local and server suites pass 124 tests, Python compilation, JavaScript syntax, and diff checks pass. A non-persistent Chinese continuation probe produced both distilled purple guidance and a specific expanded blue discussion focus in about 5.1 seconds, with unchanged counts of 40 sessions, 129 turns, 50 versions, and 9 proposals. Only `sokoban-cocreation` restarted; 8000 remained on PID 3866500 with its 18:28:58 start time. The v22 pre-deployment backup is `/root/SURF/backups/distilled-guidance-v22-20260813-1916`.
- Deployment verification on 2026-08-14 for prompt `cocreation-v24-focused-card-routing`: every reply except the first Stage 1 opening and explicitly off-topic turns is normalized into one approved one-to-three-card family. Discussion uses tentative intent, the visually separate bilingual `LET'S DISCUSS / 一起聊聊` focus, and an evidence-grounded warning in any non-empty combination; action uses manual edit alone, revision plus manual edit, or revision plus manual edit plus warning. The two families never mix and historical proposal rendering is normalized defensively. A bare or polite edit request uses Pro only when the current message or prior exchange contains a concrete revision direction; otherwise deterministic friendly copy and a single manual-edit card prevent the model from inventing a plan. Verified proposal copy remains derived from the actual before/after tile diff. Local and server suites pass 128 tests, Python compilation, local JavaScript syntax, and diff checks pass. Non-persistent probes confirmed a Pro solver-validated four-tile proposal grounded in the prior direction and the unclear-request manual-only fallback; database counts remained 42 sessions, 147 turns, 52 versions, and 10 proposals with `PRAGMA integrity_check` returning `ok`. Only `sokoban-cocreation` restarted; the 8000 backend retained PID 3866500 and its 2026-08-13 18:28:58 CST start time. The frontend cache key is `cocreation-focused-cards-20260814-16`, and the pre-deployment database/runtime backup is `/root/SURF/backups/focused-cards-v24-20260814-1052`.
- Deployment verification on 2026-08-14 for prompt `cocreation-v25-unified-friendly-cards`: manual edit, revision, tentative intent, and warning cards now share the same full pixel-card structure as the standalone `LET'S DISCUSS / 一起聊聊` focus—outer border, top tab, inset outline, spacing, and hard shadow—with color retained only for semantics. Deterministic Chinese discussion fallback text now uses friendly joint play observation rather than report-style route-analysis wording. Local and server suites pass 128 tests; Python compilation and local JavaScript syntax pass. `/`, `/health`, and `/ready` return HTTP 200, `tokenSecretConfigured` is true, and SQLite `PRAGMA integrity_check` returns `ok`. Only `sokoban-cocreation` restarted; `sokoban-backend` retained PID 3866500 and its 2026-08-13 18:28:58 CST start time. The frontend cache key is `cocreation-unified-cards-20260814-17`; the pre-deployment database/runtime backup is `/root/SURF/backups/unified-friendly-cards-v25-20260814-1200`.
- Deployment verification on 2026-08-14 for prompt `cocreation-v26-grounded-proposals-strict-relaxation`: explicit revisions now ask Pro for one to three bounded operation candidates, validate their stated source tiles and requested scope, and select the first candidate that passes the unchanged deterministic level validator and solver. The purple proposal card is distilled only from the selected proposal, preventing an earlier unrelated direction from replacing the actual edit. Strict recovery negotiation appears only after three same-Stage, same-idempotency-key, same-brief `MODEL_RESPONSE_INVALID` requests with two attempts each; it first sends an honest warning-only turn and requires an immediate explicit designer confirmation before a relaxed proposal can be generated. Network and timeout failures do not count. Local and server suites pass 132 tests; Python compilation and local JavaScript syntax checks pass. A non-persistent real Pro probe returned three internal candidates in one attempt, selected a two-cell target move matching the requested lower-area direction, and remained solver-valid with a 30-step solution. `/`, `/health`, and `/ready` return HTTP 200, `tokenSecretConfigured` remains true, and the frontend script cache key is `cocreation-strict-relaxation-20260814-20`. SQLite integrity, counts, size, and modification time remained unchanged at 45 sessions, 162 turns, 56 versions, 10 proposals, 405 audit events, and 602112 bytes. Only `sokoban-cocreation` restarted; `sokoban-backend` retained PID 3866500 and its 2026-08-13 18:28:58 CST start time. The pre-deployment database and runtime backup is `/root/SURF/backups/strict-relaxation-v26-20260814-135906`.
- The WebGL build cache key `cocreation-20260813-4` was rebuilt with Unity 2022.3.62f2c1 in 259 seconds with 0 errors and 0 warnings. The remote index and data/framework/loader/wasm assets return HTTP 200 with the expected MIME types and byte lengths; local and remote SHA-256 hashes match for all five files.
- Current verification on 2026-08-12: the unchanged main backend remains at its previously verified 144-test baseline; the independent `CoCreationPrototype/Backend` suite passes 69 tests locally and on the server. The new proposal regressions prove that identical rows cannot be saved as a proposal or accepted into a Stage, including a tampered legacy pending proposal. `node --check CoCreationPrototype/Frontend/app.js` passes locally. A reported `I think it is too easy` failure reused one idempotency key and ultimately stored exactly one user and one assistant turn; its successful reply used direct `I suspect you may...` wording. A non-persistent same-Stage live probe after the v13 deployment succeeded through Flash in one attempt and about 4.6 seconds, used direct `I think you may...` wording, offered a revision direction without generating rows, and did not write session turns. The reported Stage 2 no-op was confirmed directly in SQLite: its rows equal Stage 1 and its diff is empty; the historical record is preserved rather than silently rewritten, while v14 prevents recurrence. Two non-persistent real proposal probes after v14 confirmed the new boundary: a Pro no-op was rejected with `UNCHANGED_PROPOSAL`, and invalid player-count or unsolvable alternatives from Pro/Flash were also rejected without writing a proposal or Stage. The latest Unity verification remains `dotnet build Assembly-CSharp.csproj -v:minimal` with 0 errors and a WebGL build with 0 errors and 0 warnings; neither Unity nor 8000 was rebuilt for these 8010-only updates. A future `--no-restore` build requires the generated `Temp/obj/.../project.assets.json` to exist first.
- The last inspected code baseline before this documentation update was `1611a8f`. Do not treat a recorded commit as authoritative indefinitely; always inspect `git status` and recent history before editing, and preserve unrelated user changes.
- `WebGLBuild/` is generated output and is not committed. For a WebGL update, rebuild with Unity 2022.3.62f2c1, bump the template cache key when stale browser assets are possible, upload `WebGLBuild/`, and verify the remote index plus the data/framework/wasm responses.

## Build, Test, and Development Commands

- `python -m pip install -r Backend/requirements.txt` installs pinned backend dependencies.
- `python Backend/app.py` starts the local service on `http://127.0.0.1:8000`; check `/health`, `/ready`, and `/frontend/`.
- `python -m unittest discover -s Backend -p "test_*.py"` runs all backend unit and API-contract tests.
- `python -m pip install -r CoCreationPrototype/Backend/requirements.txt` installs the independent 8010 service.
- `python CoCreationPrototype/Backend/app.py` starts the co-creation workbench on `http://127.0.0.1:8010/`.
- `python -m unittest discover -s CoCreationPrototype/Backend/tests -p "test_*.py"` runs its persistence, prompt, API, ticket, and static-frontend tests.
- Open the project with Unity `2022.3.62f2c1`. Use **File > Build Settings > WebGL > Build** to regenerate `WebGLBuild/`.
- Run Unity tests through **Window > General > Test Runner** when adding EditMode or PlayMode coverage.

## Coding Style & Naming Conventions

Use four-space indentation in C# and Python; keep JavaScript and CSS at their existing four-space style. Follow Unity/C# conventions: `PascalCase` for types, methods, and properties; `camelCase` for fields and locals; and descriptive `*Controller`, `*Client`, or `*Manager` suffixes. Python uses `snake_case`, `PascalCase` test classes, and standard-library imports before third-party imports. No formatter is enforced, so match surrounding code and avoid unrelated reformatting.

## Testing Guidelines

Backend tests use `unittest`, FastAPI `TestClient`, and mocks for model calls. Name files `test_<feature>.py` and methods `test_<behavior>`. Add regression tests for prompt validation, API envelopes, dashboard mutations, and retry behavior. Never require a real API key or network call in tests. For gameplay changes, manually verify affected scenes and the WebGL build path.

## Commit & Pull Request Guidelines

History favors short messages such as `fix` and concise Chinese summaries; use a more specific imperative summary, for example `修复 HA 方案重试计数` or `Add dashboard deletion test`. Keep each commit focused. Pull requests should describe the user-visible change, list tests performed, link related issues, and include screenshots or recordings for scene, dashboard, or WebGL UI changes. Never commit `.env`, API keys, local logs, or generated Unity caches.
